# Clutch v1.2.6 — Product Snapshot

**Released:** 2026-07-13  
**Tag:** [v1.2.6](https://github.com/fancy1108/Clutch/releases/tag/v1.2.6)  
**Changelog:** [CHANGELOG § 1.2.6](../../CHANGELOG.md#126---2026-07-13)

## What this version is

Clutch v1.2.6 is a **macOS + Windows** patch release. It resolves a Content Security Policy (CSP) restriction inside Tauri's production config that blocked inline scripts inside preview iframes, adds graceful degradation for LLM vision failures in Design mode, and ships multi-screen generation with progressive loading animations and round transitions.

## Shipped capabilities

- All v1.2.5 capabilities (including the design presets packaging fix).
- **CSP script-src unsafe-inline rule:** Added `'unsafe-inline'` to the production `"script-src"` CSP policy inside [tauri.conf.json](../../apps/desktop/src-tauri/tauri.conf.json) to permit component picking script execution inside local `srcDoc` iframes.
- **Iframe body close tag check:** Made `</body>` tag detection case-insensitive inside `withPickerScript` to ensure the picker script is correctly appended.
- **querySelector ID safety:** Wrapped label querySelector in a `try/catch` block to prevent invalid CSS identifier syntax errors from halting script execution.
- **Canvas Zoom/Pan Over Prototype:** React Flow zoom/pan gestures restored when hovering over prototype iframe cards in the Design canvas.
- **Vision Model Fallback Degradation:** Design generation now gracefully degrades when the active LLM returns vision errors. Three independent fallback paths: `_llm_complete_vision` detects error-like LLM responses and retries text-only with local image analysis; `_generate_ui_html` detects vision errors inside generated HTML and drops the image on retry; prompt construction only references attached images when actually sent.
- **Multi-Screen Iteration:** Design `"add"` iterations parse multi-screen shorthand via `_parse_multi_screens`, planning and registering all screens upfront with sequential round entries.
- **Multi-Screen Progressive Loading:** Pre-populates placeholder screens in the manifest for smooth shimmer-animation loading sequences.
- **Screen Round Versions:** Each screen now carries its own `screenVersions` map and per-screen `roundIndex` tracking.

## Release assets

| Platform | Assets | Notes |
|----------|--------|-------|
| macOS (Apple Silicon) | `Clutch_1.2.6_aarch64.dmg`, `SHA256SUMS.txt` | CI via `release.yml` on tag |
| Windows x64 | `Clutch_1.2.6_x64-setup.exe`, `Clutch_1.2.6_x64_en-US.msi`, `SHA256SUMS_WIN.txt` | CI via `windows-build.yml` on tag |
| macOS updater | `Clutch.app.tar.gz`, `.sig`, `latest.json` | Manual `Release (updater assets)` workflow |
| Sidecar hotpatch | `sidecar-patch.json` + `orchestrator` binary | Published separately (D37) |

## Not included / known limits (v1.2.6)

- macOS DMG still **unsigned** (Gatekeeper workaround in INSTALL).
- Windows entity-machine smoke still recommended after install (see INSTALL Windows note).
- Sidecar hotpatch v1 remains **macOS-only**.
