# D41 — MCP 专用审批体验（PM 点验）

> 后端风险门 + Chat Allow/Reject 已存在（`mcp_risk` / Supervisor dock）。本项确认 UX 可读性。  
> ROADMAP 保持 ❌ 直至本用例人工通过。

## 步骤

1. Clutch Agent 绑定可写 MCP（如 `local-fs`），permission = Ask。
2. 让 Agent 写文件 → 暂停；Chat 出现 Allow / Reject；步骤条为 awaiting。
3. 预期：审批气泡或 Args 中能看到工具名与规范化路径；Reject 后不落盘；Allow 后继续。
4. Full 模式下危险命令仍会询问（与 D13 一致）。

## 结果

- [ ] PM 通过  
- [ ] 问题：____________
