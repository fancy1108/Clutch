# D41 — MCP 专用审批体验（PM 点验）

> 后端风险门 + Chat Allow/Reject 已存在（`mcp_risk` / Supervisor dock）。本项确认 UX 可读性。  
> ROADMAP 保持 ❌ 直至本用例人工通过。

## 步骤

1. Clutch Agent，permission = **Agent**（`auto_edit`；Ask 为只读硬挡，不会出 Allow）。
2. 让 Agent 执行需审批的 shell/delete（如 `rm` 非危险路径，或 `delete_file`）→ 暂停；Chat 出现 Allow / Reject；步骤条为 awaiting。
3. 预期：审批文案含工具名与路径；Reject 不执行；Allow 后继续。
4. Full 模式下危险命令仍会询问（与 D13 一致）。

## 自动化

```bash
cd services/orchestrator && uv run pytest tests/test_mcp_react.py::test_run_mcp_react_pause_on_risky_tool tests/test_mcp_risk.py tests/test_permission_rules_d13.py -q
```

## 结果

- [x] Agent 代 PM ✅ 2026-07-25 — 自动化见 `AGENT-PM-2026-07-25.md`
- [ ] 问题：____________
