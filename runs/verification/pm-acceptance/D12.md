# D12 — Git + 网页文档 · PM 验收

## 目标
Agent 可查 status/diff、审批后真实 commit；可 fetch URL 并引用要点；步骤进 D46 工具条。

## 前置
- Clutch Agent + **git 仓库**工作区 + 可用模型；权限模式 Ask（默认）

## 用例 A — Git
1. 在工作区改一个文件（或让 Agent 改）
2. 发：`用 git_status 和 git_diff 看改动，然后 git_commit 提交，message 写 d12-pm-test`
3. 期望：工具条出现 git_* 步骤；commit 前出现审批（Ask 模式）→ Allow
4. 终端/`git log -1` 可见真实 commit

## 用例 B — web_fetch
1. 发：`用 web_fetch 打开 https://example.com ，用一句话总结页面要点`
2. 期望：工具步骤 `web_fetch` 成功；回复引用页面内容（例：Example Domain）

## 自动化自测（Agent 已跑）
```bash
cd services/orchestrator && uv run pytest tests/test_git_web_d12.py -q
```
