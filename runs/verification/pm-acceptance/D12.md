# D12 — Git + 网页文档 · PM 验收

## 结果

- [x] PM 通过（2026-07-25）
- **用例 A：** 审批后真实 commit — `my-video` 仓库 `bcfc099` / message `d12-pm-test`（25 files）
- **用例 B：** `web_fetch` 成功并总结页面要点

## 用例 A — Git ✅
1. 发：`用 git_status 和 git_diff 看改动，然后 git_commit 提交，message 写 d12-pm-test`
2. Ask 模式出现 `git_commit` 审批 → Allow
3. Agent 回报已提交；工作区 `git log -1` 可见 `d12-pm-test`

## 用例 B — web_fetch ✅
1. 发：`用 web_fetch 打开 <URL>，用一句话/要点总结`
2. 工具步骤成功；回复引用页面内容

## 自动化
```bash
cd services/orchestrator && uv run pytest tests/test_git_web_d12.py -q
```
