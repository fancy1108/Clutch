# D14 — 外挂 grok CLI · PM 验收

## 目标
引擎列表可选 Grok；PATH 有 `grok` 时可路由；缺失时清晰不可用；轨迹进 Terminal/steps。

## 步骤
1. Settings → Tools：扫描到 **Grok CLI**（`grok-cli`）或显示未安装。
2. 新建 Agent，`agentType` = `grok-cli`，连接工具。
3. 发 Chat → Terminal 有 `[GROK CLI]` 或 generic CLI 行；无 grok 时失败信息可读。
4. 不替代 D1–D13 自研 Clutch Agent 能力。

## 自动化
```bash
cd services/orchestrator && uv run pytest tests/test_grok_cli_d14.py -q
```

## 结果

- [x] Agent 代 PM ✅ 2026-07-25 — 自动化见 `AGENT-PM-2026-07-25.md`
