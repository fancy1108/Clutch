# D24 — 诊断 MVP · PM 验收

## 目标
可选 `diagnostics` 工具运行 tsc/ruff/py_compile；结果注入下一轮 + Chat issues 条。

## 步骤
1. 在工作区引入 Python 语法错误文件。
2. 让 Agent 调用 `diagnostics`（或你手动触发工具）。
3. 下一轮输入前 Chat 出现 **Code diagnostics** 条（`data-testid=chat-diagnostics-strip`）。
4. Agent 下一轮应能针对错误修复。

## 自动化
```bash
cd services/orchestrator && uv run pytest tests/test_diagnostics_d24.py -q
```
