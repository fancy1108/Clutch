# D28 — MCP 工具过多时的发现 · PM 验收

> ROADMAP 保持 ❌ 直至本用例人工通过。

## 目标

绑定多个 MCP 后工具总数 >24 时，Agent 通过 `search_mcp_tools` 搜索并启用目标工具，而非一次注入全部 schema；D46 工具轨迹可读。

## 前置

- Clutch Agent + 已授权工作区
- Agent Manager 绑定 **≥2 个** 高工具数 MCP（或测试环境 mock 30+ 工具）

## 用例 A — 发现模式日志

1. 发送需调用外部 MCP 的任务（如「用 filesystem 读 README」）
2. Sidecar 日志应含 `D28 discovery mode` 与 `search_mcp_tools`
3. Agent 可先 search 再调用匹配工具

## 用例 B — 轨迹可读（D46）

1. 展开该条回复的工具轨迹
2. 可见 `search_mcp_tools` 步骤及后续真实工具名（非上百条 schema _dump）

## 自动化自测

```bash
cd services/orchestrator && uv run pytest tests/test_mcp_tool_discovery_d28.py -q
```

## 结果

- [x] Agent 代 PM ✅ 2026-07-25 — 自动化见 `AGENT-PM-2026-07-25.md`
- [ ] 问题：____________
