# D27 — 模式切换 · PM 验收

## 目标
Chat 通过输入框底栏 **模式 pill** 切换 **Agent / Plan / Full / Ask**（对标 Cursor）；Ask 禁止写文件；默认 Agent 可写。

## 说明（2026-07-25）
- Explore 并入 **Ask**；Edit 显示名为 **Agent**（∞）
- 菜单顺序：Agent → Plan → Full → Ask

## 步骤
1. 默认 pill 为 **Agent**；发写文件任务应可改（或 shell 审批）
2. 切到 **Ask**，发「写一个 hello.txt」→ 写操作被挡（Ask Mode blocked）
3. 切回 **Agent** / **Full** → 可写

## 结果

- [x] Agent 代 PM ✅ 2026-07-25 — 自动化见 `AGENT-PM-2026-07-25.md`
