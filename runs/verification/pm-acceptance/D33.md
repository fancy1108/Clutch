# D33 — 富文件阅读 · PM 验收

> ROADMAP 保持 ❌ 直至本用例人工通过。

## 目标

`read_file` 可读工作区 **图片**（本地 OCR/分析）与 **PDF**（`pdftotext` 在 PATH 时）。

## 前置

- Clutch Agent + 已授权工作区
- 工作区放一张含文字的截图 + 一份文本 PDF

## 用例 A — 图片

1. 发送：「用 read_file 读 `screenshot.png` 里写了什么」
2. 期望：返回 OCR/分析片段，助手能复述图中文字（非瞎编）

## 用例 B — PDF（有 poppler）

1. macOS：`brew install poppler`（若未装）
2. 发送：「read_file `doc.pdf` 并总结第一段」
3. 期望：提取正文并摘要

## 用例 C — PDF（无 pdftotext）

1. 在无 poppler 环境重复
2. 期望：可读错误提示安装 poppler，不崩溃

## 自动化自测

```bash
cd services/orchestrator && uv run pytest tests/test_rich_read_d33.py -q
```

## 结果

- [ ] PM 通过
- [ ] 问题：____________
