# D10∥D48 — 子任务委派 · PM 验收

## 目标
父 Chat 可见嵌套子任务卡（explore / implement）；子失败主可见；先探查再改。

## 前置
- Clutch Agent + 已授权工作区 + 可用模型

## 步骤
1. 新开 Chat，发：`先用 explore 子任务调研工作区有哪些入口文件，再 implement 写一个 README_SNIPPET.md 一句话说明`
2. 观察运行中：Thinking / Working 区域出现 **Subtasks** 嵌套卡（`data-testid=subtask-cards`），至少一条 `explore` 状态 running→done
3. 完成后：助手气泡下仍有 Subtasks 卡，含摘要；若有 implement，可见其状态
4. （失败可见）可再发：`用 implement 子任务去改一个不存在的路径 /no/such/file.py`，期望子卡 status=failed，错误/摘要在父气泡可见

## 期望
- [ ] 出现嵌套 Subtasks 卡（非仅日志）
- [ ] explore 与 implement 类型可区分
- [ ] 子失败时父卡可见 failed
- [ ] 工具步骤可展开（Show steps）

## 自动化自测（Agent 已跑）
```bash
cd services/orchestrator && uv run pytest tests/test_subagent_d10.py tests/test_mcp_react.py -q
```
