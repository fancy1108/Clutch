# D29 — Goal 跟踪（PM 点验）

> ROADMAP 保持 ❌ 直至本用例人工通过。

## 前置

- 工作区已授权；Chat 选择 **Clutch Agent**

## 步骤

1. 让 Agent 调用 `goal_write`，标题 **修登录**，`progress` 约 20。
2. Chat 顶部出现 **目标条**（标题 + 进度条 + 百分比）。
3. 再调用 `goal_write` 将 `progress` 提到 60 — 条上百分比与进度条更新。
4. 最后 `goal_write` 设 `done: true`（或 `progress: 100`）— 目标条关闭。

## 自测证据

```bash
cd services/orchestrator && uv run pytest tests/test_goal_tracking_d29.py -v
cd apps/desktop && pnpm test -- src/components/GoalBarView.test.ts
```

## 结果

- [ ] PM 通过  
- [ ] 问题：____________
