# Agent 代 PM 自验收报告（2026-07-25）

> 按各 `D*.md` 自动化命令执行；失败已修；UI 真连环境不足项标 Skip。

## Wave A — D14–D36

| 结果 | 证据 |
|------|------|
| **59 passed, 1 skipped** (pytest 包) | `wave-a-pytest.log` |
| **20 passed** (vitest 包) | `wave-a-vitest.log` |

含：D14–D24、Cap-D25、D26–D36（headless）、D27、D29–D35 等用例内命令。

## Wave B — MCP / Chat UX

| 结果 | 证据 |
|------|------|
| **23 passed** (permission/mcp pytest) | `wave-b-pytest.log` |
| vitest D40/D51 **8 passed** | `wave-b-vitest.log` |
| D52 门禁 `check-capability-ui-table.sh` + doc-drift **OK** | `wave-b-d52.log` / `wave-b-doc-drift.log` |
| D41 用例已按 D54 改写（Agent 下 shell/delete 审批，非 Ask 写文件） | `D41.md` |
| D45 PRODUCT_INTRO 抽查：stdio / Test connection / 徽章 / Capability 表 | 文档 grep 一致 |

## Wave C — E2E

| 套件 | 结果 | 说明 |
|------|------|------|
| API E2E（含 MVP closed-loop） | ✅ 6/6 | 修 `route_engine` sandbox FAKE_LLM（`0c3462d`/`61c22ec`） |
| Hybrid concurrent API | ✅ 1/1 | |
| Desktop Tauri Playwright | **Skip（环境）** | Vite `:3000` 180s 未起；见 `wave-c-run-e2e.log` |
| Real acceptance (`e2e-preflight`) | **Skip（环境）** | Ollama `:11434` 不可达 |

## 代码修复（本轮）

- `fix(e2e): short-circuit route_engine in sandbox FAKE_LLM` — `0c3462d`
- `fix(e2e): limit FAKE_LLM short-circuit to workflow flow source` — `61c22ec`
