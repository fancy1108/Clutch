# D19 — 思考流 + 工具进度（PM 点验）

> ROADMAP 保持 ❌ 直至本用例人工通过。与 **D46** 共用同一 Chat 活动区，无第二套互斥条。

## 前置

- Clutch Agent + 支持 `reasoning_content` 的模型（或 mock 自测已跑 pytest）
- 可选：触发 `run_terminal_cmd` 的长输出命令

## 步骤

1. 发起需多步推理的任务；**Working** 气泡内 D46 工具轨迹上方出现可折叠 **「思考」**（`agent-live-reasoning`）。
2. 点击展开/收起，可见模型 `reasoning_content` 文本流式更新（同回合内追加）。
3. 执行较长 shell 命令后，展开对应 **Run …** 步骤，detail 区域应含输出片段（非仅命令行）。
4. 回合结束后思考条随 `live_reasoning` 清空；历史气泡仍保留已 seal 的 `toolSteps`。

## 自测证据

```bash
cd services/orchestrator && uv run pytest tests/test_thinking_stream_d19.py -q
# → passed

cd apps/desktop && pnpm test -- src/services/agentLiveReasoning.test.ts
# → passed
```

## 结果

- [x] Agent 代 PM ✅ 2026-07-25 — 自动化见 `AGENT-PM-2026-07-25.md`
- [ ] 问题：____________
