# D44 — 一键启用工作区工具（PM 点验）

> ROADMAP 保持 ❌ 直至本用例人工通过。

## 步骤

1. Agent Manager → 编辑 Clutch Agent → Module 4。
2. 若未勾选 `local-fs`：点 **「启用工作区文件工具」**（`mcp-enable-workspace-tools`）。
3. 预期：`local-fs` 复选框变为勾选；文案变为「已启用工作区文件工具」。
4. 保存 Agent → Chat 用 Clutch Agent 让其 list/读工作区文件（Hub filesystem 工具可用；builtins 仍始终可用）。

## 结果

- [ ] PM 通过  
- [ ] 问题：____________
