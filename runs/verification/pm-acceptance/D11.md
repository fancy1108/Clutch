# D11 — 后台命令 · PM 验收

## 目标
后台跑命令、看输出、完成通知、可杀；前台仍可聊天。

## 前置
- Clutch Agent + 已授权工作区 + 可用模型

## 步骤
1. 新开 Chat，发：`用 run_terminal_cmd 后台跑 sleep 20 && echo BG_DONE（background=true），然后马上回答：1+1=?`
2. 期望：Agent 很快答出 `2`（不必等 sleep 结束）
3. 输入栏上方出现 Background jobs 芯片（`data-testid=bg-job-…`），状态 running
4. 点 **View output** 可看输出缓冲；可点 Kill 终止
5. 若不杀，等结束后：芯片变 done，且 Chat 有完成提示（Supervisor/System）

## 期望
- [ ] 后台启动后仍可继续问答
- [ ] 任务条可见 + View output
- [ ] Kill 或完成通知至少一种有效

## 自动化自测（Agent 已跑）
```bash
cd services/orchestrator && uv run pytest tests/test_bg_jobs_d11.py -q
```
