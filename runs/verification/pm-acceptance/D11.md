# D11 — 后台命令 · PM 验收

## 结果

- [x] PM 通过（2026-07-25）
- 后台答 `1+1=2`；RUNNING 条；完成后进对话时间线；Kill 即时反馈、无红 toast

## 验收对照

| 期望 | 结果 |
|------|------|
| 后台启动后马上答 `1+1=?` | ✅ |
| 任务条 + View output / Kill | ✅ 仅 RUNNING 在底栏 |
| 完成进对话 | ✅ Supervisor + job 卡（DONE / 输出） |
| Kill 即时 | ✅ 乐观 Stopping；无失败 toast |

## 同批修复
- dock 增高后自动上滚；底栏不挡气泡
- 结束后底栏消失，卡封进 Chat（`msg.bgJob`）
- Kill 乐观 UI；killed 不弹失败 toast；进程回收离线线程

## 自动化
```bash
cd services/orchestrator && uv run pytest tests/test_bg_jobs_d11.py tests/test_bg_jobs_monitor_d26.py -q
```
