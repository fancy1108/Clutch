# D13 — 权限规则 · PM 验收

## 结果

- [x] PM 通过（2026-07-25）— **用例 A**（Full 下危险命令仍强问）
- [ ] 用例 B / C — **PM 不点验**（自动化保留）

## 目标
危险命令强问；允许/询问/拒绝规则；可清除记住的批准。

## 用例 A — 危险命令强问 ✅
1. 权限模式切到 **Full**
2. 发：`用 run_terminal_cmd 执行：rm -rf ./tmp-d13-test`
3. 期望：仍弹出审批（不会因 Full 静默执行）

## 用例 B — 拒绝规则（跳过人工）
自动化：`pytest tests/test_permission_rules_d13.py -q`

## 用例 C — 清除记忆（跳过人工）
Clear remembered approvals → 再次同类工具仍会询问

## 模式说明（同批）
底栏：**Agent**（默认，可改）/ Plan / Full / **Ask**（只读对话）。Explore 已并入 Ask。

## 自动化
```bash
cd services/orchestrator && uv run pytest tests/test_permission_rules_d13.py tests/test_permission_explore_d27.py -q
```
