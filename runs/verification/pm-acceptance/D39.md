# D39 — 传输诚实（PM 点验）

> ROADMAP 保持 ❌ 直至本用例人工通过。本交付为 **诚实 UI**：stdio 可跑；SSE 禁用/拒绝注册（未实现远程 runtime）。

## 步骤

1. Settings → MCP → Clutch Tab 注册表单：
   - Transport 下拉 **sse 为 disabled**，旁注 unavailable；
   - 文案提示「当前仅 stdio 可运行」；
   - 可选 Env：`KEY=value` 每行。
2. 尝试用 API 或 raw JSON 以外的 UI 选 sse —— 应无法提交。
3. （API）`POST /api/mcp/servers/register` with `"transport":"sse"` → **400** 且文案说明改用 stdio。
4. 用 stdio + 可选 env 注册成功。

## 自测证据

```bash
cd services/orchestrator && uv run pytest tests/test_mcp_registry.py tests/test_mcp_probe_d38.py -q
# → 8 passed
```

## 结果

- [x] Agent 代 PM ✅ 2026-07-25 — 自动化见 `AGENT-PM-2026-07-25.md`
- [ ] 问题：____________
