# D30 — 会话总览看板（PM 点验）

> ROADMAP 保持 ❌ 直至本用例人工通过。

## 前置

- 同一工作区至少 **2 条** Chat 会话历史（可先发两条不同消息生成）

## 步骤

1. 在 Chat 输入栏上方点击 **Sessions / 会话** 打开总览看板。
2. 列表展示各会话标题、所属项目，以及状态徽章：**Running / Done / Idle**。
3. 当前会话高亮；点击另一条会话 — 应用切回该 `run_id` 并加载历史消息。
4. （D23 fork 父子标识可后续验收；本项仅看板 + 点回。）

## 自测证据

```bash
cd apps/desktop && pnpm test -- src/components/SessionOverviewBoard.test.ts
```

## 结果

- [ ] PM 通过  
- [ ] 问题：____________
