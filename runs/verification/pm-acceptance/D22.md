# D22 — 用量看板 · PM 验收

## 目标
本局 token/工具步数可点开查看；历史 session 列表展示各 run 用量，与 D9 实时条一致。

## 前置
- Clutch Agent + 已授权工作区 + 可用模型
- 侧栏至少 1 条历史 Chat session（可选）

## Composer 提示（v1.2.9+）
用量入口在输入框 **+** 菜单 → **Usage**（`data-testid="chat-run-stats"`），不再常驻输入框上方。

## 用例 A — 本局用量 + 看板
1. 新开 Chat，发一条会跑工具的任务（如「列出工作区根目录文件」）
2. 打开 **+** 菜单 → 可见 **Usage** `Steps n/m · ~N tok`（`chat-run-stats`）
3. **点击**该项
4. 期望：弹出 **Usage dashboard**（`data-testid="usage-dashboard"`），顶部显示本局 steps/tok 与菜单中数字一致
5. 再点关闭或点遮罩外，看板收起

## 用例 B — 跑一轮后数字增加
1. 记下看板中本局 steps/tok
2. 再发一条会触发至少 1 次工具的消息
3. 期望：**+ → Usage** 与看板本局行 steps/tok **均增加**（不必精确等于供应商计费）

## 用例 C — 历史 session 用量
1. 打开看板，表格列出近期 session（标题 + 日期 + Steps + ~tok）
2. 当前 session 行高亮；切换侧栏其它 session 后，对应用量字段来自历史记录（若该 session 曾跑过工具）

## 自动化自测（Agent 已跑）
```bash
cd services/orchestrator && uv run pytest tests/test_usage_d22.py -q
pnpm exec vitest run apps/desktop/src/components/UsageDashboard.test.ts
```
