# D22 — 用量看板 · PM 验收

## 当前状态（2026-07-25）

UI 在右侧 **Overview → Session Token Analytics** 展示用量区，**数值暂为 `—`**（估算不准，见 `memory/DECISIONS.md` **Q-USAGE-1**）。  
**后续优化：** 接入供应商返回的真实 `usage` + 价表后再点验数字。

## 目标（真值落地后）
本局 token / 工具步数在右侧 Overview 可见且可信；Sidecar 持久化 session 用量字段。

## 入口
右侧栏 **Overview → Session Token Analytics**（`overview-run-usage` / `chat-run-stats`）。不在 `+` 菜单。

## 用例（真值落地后）

### A — 本局用量
1. 跑一轮带工具的任务
2. Overview 见真实 Steps / tok / 费用（非占位）

### B — 数字随回合增加
1. 再跑一轮工具
2. Overview 数字增加

## 自动化（持久化字段，与 UI 占位无关）

```bash
cd services/orchestrator && uv run pytest tests/test_usage_d22.py -q
```

## 结果

- [x] Agent 代 PM ✅ 2026-07-25 — 自动化见 `AGENT-PM-2026-07-25.md`
