{
  "name": "@clutch/e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test -c playwright.config.ts",
    "test:api": "playwright test -c playwright.config.ts --project=api",
    "test:desktop": "playwright test -c playwright.config.ts --project=desktop"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0",
    "@srsholmes/tauri-playwright": "^0.4.1"
  }
}
